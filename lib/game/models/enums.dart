enum GameRound {
  round1,
  round2,
  round3,
}

enum ShellType {
  live,
  blank,
}

enum ItemType {
  inverter,
  cigarettes,
  burnerPhone,
  adrenaline,
  magnifyingGlass,
  beer,
  handSaw,
  handcuffs,
}

enum ShotTarget {
  self,
  opponent,
}

enum RoundWinner {
  player1,
  player2,
}
